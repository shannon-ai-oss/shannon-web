# Contributing

Thanks for your interest in Shannon Web.

## Getting Started
- Install Node.js 22+.
- Copy `.env.local.example` to `.env.local` and set `VITE_CHAT_API_URL`.
- Run `npm install` then `npm run dev`.

## Style & Tests
- Keep changes focused and well‑scoped.
- Run `npm run build` before opening a PR.
- If you add new features, include a short note in the PR description.

## Submitting Changes
- Use clear commit messages.
- Explain breaking changes and migrations in your PR.

## Security
If you discover a security issue, follow `SECURITY.md` instead of opening a public issue.
